export interface Customer {
  uid: string;
  name: string;
  email: string;
  phone: string;
}

export interface CustomerDetail {
  address: string;
  city: string;
  state: string;
  customerId: string;
}
